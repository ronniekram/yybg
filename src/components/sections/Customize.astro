---
import { Image } from "astro:assets";
import alchemist from "@assets/graphics/gif/alchemist.gif";
import customize from "@assets/graphics/customize.webp";
---

<section>
  <div class="wrapper base">
    <div class="left">
      <Image src={alchemist} alt="" loading="lazy" class="w-full h-auto mix-blend-luminosity" />
    </div>
    <div class="right h-full flex flex-col justify-center gap-y-1">
      <Image src={customize} alt="" class="w-full h-auto" loading="lazy" />
      <h2 class="heading">Customize Your Homunculi</h2>
      <p class="p-body">
        Discover new card synergies while customizing your build with gear won in both the
        single-player campaign and online. Turn your Bloodgun into a deathray or add poking distance
        to your sword, each item offering a unique gameplay mechanic to complement your build.
      </p>
    </div>
  </div>
</section>

<style>
  section {
    @apply py-10 xl:py-16 place-content-center bg-red-BL;
  }

  .left {
    @apply col-span-6 sm:col-span-3 lg:col-span-4 xl:col-span-3 2xl:col-span-4 order-2 sm:order-1;
  }

  .right {
    @apply col-span-6 sm:col-span-5 lg:col-span-6 xl:col-start-5 2xl:col-span-7 2xl:col-start-6 order-1 sm:order-2;
  }

  .heading {
    @apply text-red-500 font-bold text-center text-balance sm:text-left text-2xl md:text-3xl lg:text-4xl xl:text-5xl 3xl:text-6xl pt-2;
  }

  .p-body {
    @apply text-center sm:text-left text-sm lg:text-base xl:text-lg 2xl:text-xl;
  }
</style>
